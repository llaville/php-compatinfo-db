---
name: GitHub-Pages

on:
    push:
        branches:
            - master
            - "6.0"
        paths:
            - docs/**
    pull_request:
    workflow_dispatch:

jobs:
    deploy:
        uses: llaville/.github/.github/workflows/gh-pages.yml@uml
        with:
            destination-dir: "6.0"
            force-orphan: false
            hook-script: "resources/gh-pages-hook.sh"
            graph-uml: true
            php-version: "8.1"
