---
name: GitHub-Pages

on:
    push:
        branches:
            - master
            - "5.14"
        paths:
            - docs/**
    pull_request:
    workflow_dispatch:

jobs:
    deploy:
        uses: llaville/.github/.github/workflows/gh-pages.yml@uml
        with:
            destination-dir: "5.14"
            force-orphan: false
            hook-script: "resources/gh-pages-hook.sh"
            graph-uml: true
